const example = ['apple', 'banana', 'mango', 'kiwi', 'melon', 'grape'];

console.log(example.length);
for (let index = 0; index < example.length; index++) {
    const element = example[index];
    console.log(element);
}
